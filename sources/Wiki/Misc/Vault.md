Tips and tricks to interact with Hashicorp Vault using the CLI.

## Policies

Listing:

```bash
> vault policy list
```

> [!TIP]
> The command above is the equivalent as running:
> ```bash
> > vault list sys/policy
> ```

## AppRole

Listing:
```bash
> vault list auth/approle/role
```
